[env]

path = [
    "$HOME/bin",
    "$GOPATH/bin",
    "$HOME/.local/bin",
    "$HOME/.config/git/bin",
    "$TCONF/bin",
    "$TCONF/local/bin",
    "$TCONF/priv/bin",
]

[env.vars]

GITHUB_USER = "tallclair"
GOPATH = "$HOME/go"
TCONF = "$HOME/tconf"
