# Common environment variables
{{ range $key, $value := .env.vars -}}
set -gx {{ $key }} {{ $value | quote }}
{{ end -}}

# Common path fragments
{{ range .env.path -}}
if test -d {{ . | quote }}
    fish_add_path --path {{ . | quote }}
end
{{ end -}}
